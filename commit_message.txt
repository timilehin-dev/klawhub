🧹 Remove dead code `tavily_keys` in config

🎯 **What:** Removed the `tavily_keys` property from `src/config.py`. Updated `src/integrations/providers/tavily/client.py` to correctly fetch keys directly instead of accessing the dead code property.
💡 **Why:** To improve maintainability and readability by getting rid of dead code.
✅ **Verification:** Ran `PYTHONPATH=. python3 src/scripts/verify_tavily_lightpanda.py` which executes assertions that test the Tavily integration directly. Also ran the code review tool and it flagged no issues with the logic changes. I have removed the pycache files from being committed as well.
✨ **Result:** A cleaner codebase where useless unused property is gone but everything functions normally without regressions.
